# B-ORDER-20260422-1329-002 交付说明

## 1. 改动文件清单

- `doris-api/requirements.txt`
- `doris-api/vanna_compat.py`
- `doris-api/vanna_doris.py`
- `doris-api/tests/conftest.py`
- `doris-api/tests/test_vanna_compat.py`

## 2. 兼容策略说明

- 依赖层：将 `vanna` 升级到 `2.0.2`，保持其余查询链路逻辑不重写。
- 代码层：新增本地 shim `vanna_compat.py`。
- `VannaBase` 解析顺序：
  1. 优先 `vanna.legacy.base`（对齐 Vanna 2 官方 Legacy 迁移路径）
  2. 回退 `vanna.base`（兼容 0.x/部分包布局）
- 查询主链路契约保持不变：`main.py`、`vanna_doris.py` 的外部 API 行为不改。
- 测试层补充 `test_vanna_compat.py`，锁定兼容导入路径行为。

## 3. 回归命令与结果

- 环境确认：
  - `python -m pip show vanna` -> `Version: 2.0.2`
  - `importlib` 检查 -> `vanna.legacy.base=True`, `vanna.legacy.adapter=True`
- 回归命令：
  - `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -o addopts= doris-api/tests/test_vanna_compat.py doris-api/tests/test_query_intelligence_contract.py doris-api/tests/test_phase3_orchestration.py doris-api/tests/test_llm_resource_flow.py doris-api/tests/test_post_query_hook.py doris-api/tests/test_phase2_agents.py doris-api/tests/test_phase4_production.py doris-api/tests/test_phase5_vector_search.py doris-api/tests/test_security_and_phase1.py doris-api/tests/test_golden_runner.py -k "not mcp_server"`
- 结果：
  - `44 passed, 3 deselected`
  - 1 条上游 warning（`vanna` 内部 Pydantic deprecation），不影响当前查询契约。

## 4. 当前仍未吃到的 Vanna 2 新能力

- `Agent` / `ToolRegistry` 原生编排
- `LegacyVannaAdapter` 接入到新服务端（FastAPI/Flask）与 `<vanna-chat>` streaming UI
- user-aware 权限、分组工具访问、审计链路
- 富流式组件响应（图表/状态卡等）

## 5. 风险说明

- `requirements.txt` 中 `pydantic==2.9.2` 与 `vanna 2.0.2` 在全新安装场景可能触发版本冲突（运行时会被 `vanna` 拉高到 `2.13.x`）；后续建议在统一依赖工单中收敛版本上限。
- 当前采用 compatibility shim，能保证“先跑在 2.0.2 包上”，但并未引入 Vanna 2 原生 Agent 模型能力，后续需单开专项迁移。
