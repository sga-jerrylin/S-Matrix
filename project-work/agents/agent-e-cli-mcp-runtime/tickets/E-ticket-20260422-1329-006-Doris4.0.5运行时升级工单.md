# E-ticket-20260422-1329-006-Doris4.0.5运行时升级工单

## 工单状态

待执行

## 对应指令单

`E-order-20260422-1329-006-Doris4.0.5运行时升级指令单.md`

## 目标

将 Doris 本地运行时从 `4.0.0` 升级到 `4.0.5`，保持当前本地 Docker 联调环境可重复初始化、单 backend、alive 门禁有效、保存数据源可用。

## 边界

只改运行时 owner 文件，不碰查询业务逻辑与 Vanna 迁移。

## 验收

1. FE/BE = `4.0.5`
2. 4 服务 healthy
3. backend 唯一且 `Alive=true`
4. `dc smoke` 通过
5. 保存数据源 API 通过

