# agent-e Docker 环境拉起与联调前健康检查交付说明

- 指令单：`E-ORDER-20260419-1611-003`
- 工单：`E-TICKET-20260419-1611-003`
- 时间：`2026-04-19`
- 工作目录：`E:\SGA-DorisClaw`

## 1) 本次改动文件清单

- `E:\SGA-DorisClaw\init.ps1`
- `E:\SGA-DorisClaw\init.sh`

说明：仅做 runtime 启动稳态修正（健康等待、FE MySQL 就绪等待、BE 注册重试、失败即退出），未改业务逻辑、未改数据基础层语义。

## 2) 实际执行命令清单

```powershell
docker version
powershell -ExecutionPolicy Bypass -File .\init.ps1
docker compose up -d --build --remove-orphans
python doris-api/dc.py smoke
curl.exe -s -o nul -w "%{http_code}" http://localhost:38018/api/health
curl.exe -s -o nul -w "%{http_code}" http://localhost:35173
docker compose ps
```

补充：在首次失败阶段，执行过镜像预拉取与诊断命令（`docker pull node:20-alpine`、`docker pull python:3.11-slim`、`docker pull nginx:alpine`、`python doris-api/dc.py doctor`、`python doris-api/dc.py smoke --compose-up`）用于定位和消除 Docker Hub 拉取超时阻塞。

## 3) `docker compose ps` 结果摘要

当前 4 个服务均为 `Up ... (healthy)`：

- `smatrix-fe`（8030/9010/9030）
- `smatrix-be`（8040/9050/9060）
- `smatrix-api`（8000，对外 38018）
- `smatrix-frontend`（80，对外 35173）

## 4) 健康检查结果摘要

- API 健康端点：`http://localhost:38018/api/health` -> `200`
- 前端可达：`http://localhost:35173` -> `200`

## 5) smoke 结果摘要

执行：`python doris-api/dc.py smoke`

结果：`success: true`，关键检查通过：

- 容器健康：`smatrix-fe` / `smatrix-be` / `smatrix-api` / `smatrix-frontend`
- `fe_bootstrap` 200
- `be_health` 200
- `api_health` 200
- `frontend` 200
- `api_unauthorized`（未授权正确返回 401）
- `api_auth`（鉴权后接口成功）
- `system_tables` 就绪

## 6) 阻塞项与风险说明

已定位并消除的阻塞：

1. Docker daemon 未就绪（最初 `docker version` 无法连接）。
2. Docker Hub 拉取元数据/镜像出现 TLS timeout/EOF，导致 `docker compose up` 首次失败。
3. 初始化脚本中 FE MySQL 探测使用 `127.0.0.1` 在容器内场景不稳定，导致 BE 注册阶段偶发失败。

当前残余风险（非阻塞）：

- 外网到 Docker Hub 抖动时，冷启动可能再次触发拉取超时；建议保留镜像预拉取作为故障排查第一步。

## 7) 联调前状态结论

结论：**当前环境已达到“等待真实数据源接入”的可联调状态**。

边界声明：本轮仅完成 runtime 拉起与健康收口，不代表真实数据源、真实业务链路已完成联调。
