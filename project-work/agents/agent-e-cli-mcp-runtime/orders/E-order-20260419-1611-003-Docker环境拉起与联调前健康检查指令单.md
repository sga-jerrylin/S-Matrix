# agent-e Docker环境拉起与联调前健康检查指令单

- 指令单编号：`E-ORDER-20260419-1611-003`
- 时间：`2026-04-19 16:11:10 +08:00`
- agent：`agent-e`
- 授权级别：`模块内可使用 sub-agent`
- 对应工单：`E-TICKET-20260419-1611-003`

## 你要做什么

- 负责把 DorisClaw 当前 Docker 环境拉起来，进入“可联调”状态。
- 优先复用现有启动入口与 runtime/compose 脚本，不重写业务逻辑。
- 完成联调前的最小健康检查，确认：
  - `docker compose` 服务已启动
  - API 健康可达
  - 前端可达
  - `dc smoke` 至少能跑到当前环境边界
- 如果环境拉起失败，必须收敛成明确阻塞项，而不是给口头“已启动”。

## 你不要做什么

- 不接入真实数据源
- 不做真实业务联调
- 不修改查询、洞察、预测业务逻辑
- 不越权修改数据基础层、前端业务实现
- 不把“容器起来了”冒充成“全功能可用”

## 本轮必须收口的点

1. Docker 环境拉起
- 使用项目当前推荐入口优先处理：
  - Windows 优先：`init.ps1`
  - 或等价的 `docker compose up -d`
- 如 Docker daemon 未启动，要明确记录阻塞，不得伪造成功。

2. 健康检查
- 输出 `docker compose ps`
- 验证 API 健康端点
- 验证前端可达
- 运行一次当前 smoke 流程，确认卡点或通过点

3. 联调前状态说明
- 明确当前环境是否已经达到“等待真实数据源接入”的状态
- 如果还没达到，给出阻塞项、日志位置、建议下一步

## 建议负责文件

- `docker-compose.yml`
- `init.ps1`
- `init.sh`
- `scripts/smoke_docker.sh`
- `doris-api/dc_runtime/doctor.py`
- 如有必要，可修改与启动/健康检查直接相关的脚本或 runtime 壳层

## Sub-agent 授权

- 允许在 Docker / compose / 启动脚本 / runtime 健康检查壳层范围内使用 sub-agent
- 不允许 sub-agent 越权修改查询、洞察、预测业务实现
- 最终交付由你主责任人统一复核

## 交付要求

- 改动文件清单
- 实际执行命令清单
- `docker compose ps` 结果摘要
- 健康检查结果摘要
- smoke 结果摘要
- 阻塞项或风险说明

## 验收标准

- 容器已实际启动，或阻塞原因已被明确定位
- API 健康可验证
- 前端可达状态可验证
- smoke 已执行并有明确结果
- 结论能支持下一步“接入真实数据源后做整轮联调”
