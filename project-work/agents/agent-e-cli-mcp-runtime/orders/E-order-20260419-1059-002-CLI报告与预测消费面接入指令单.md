# agent-e CLI报告与预测消费面接入指令单

- 指令单编号：`E-ORDER-20260419-1059-002`
- 时间：`2026-04-19 10:59:25 +08:00`
- agent：`agent-e`
- 授权级别：`模块内可使用 sub-agent`
- 对应工单：`E-TICKET-20260419-1059-002`

## 你要做什么

- 把 `dc report` 从占位能力推进成真实可用能力。
- 在 `agent-c` 的 forecast MVP 输出稳定后，把 `dc forecast` 接成真实消费面。
- 继续坚持统一 runtime，不重写业务逻辑，只代理现有后端契约。
- 让 `SGA-Molt` 后续可以通过 CLI / MCP 直接消费 report 和 forecast。

## 你不要做什么

- 不自己定义 report / forecast 业务 schema。
- 不抢先扩 forecast 输出。
- 不重写分析逻辑和预测逻辑。
- 不越权改前端和数据基础层。

## 本轮必须收口的点

1. `dc report`
- 支持报告列表、报告详情、报告摘要读取
- 支持稳定 JSON 输出

2. `dc forecast`
- 跟随后端 MVP contract
- 支持请求 forecast
- 支持稳定 JSON 输出
- 错误时返回稳定错误码和消息

3. MCP/runtime
- 只在已有后端 contract 稳定后再补工具面
- 不要制造第二套独立协议

## 建议负责文件

- `doris-api/dc_runtime/cli.py`
- `doris-api/dc_runtime/client.py`
- `doris-api/dc_runtime/mcp_server.py`
- `dc.py`
- 对应测试文件

## Sub-agent 授权

- 允许在 CLI / runtime / MCP 壳层范围内使用 sub-agent。
- 不允许 sub-agent 越权修改预测业务实现和数据基础层定义。

## 交付要求

- 改动文件清单
- `dc report` 命令面说明
- `dc forecast` 命令面说明
- MCP / runtime 接入说明
- 风险说明
- 回归命令与结果

## 验收标准

- `dc report` 可真实读取报告能力
- `dc forecast` 可真实消费预测能力
- runtime / MCP 不分叉业务逻辑
