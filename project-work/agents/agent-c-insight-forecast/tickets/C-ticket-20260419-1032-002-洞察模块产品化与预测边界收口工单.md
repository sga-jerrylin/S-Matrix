# agent-c 洞察模块产品化与预测边界收口工单

- 工单编号：`C-TICKET-20260419-1032-002`
- 时间：`2026-04-19 10:32:35 +08:00`
- agent：`agent-c`
- 批次：`Batch 2`
- 状态：`已启动`
- 对应指令单：`C-ORDER-20260419-1032-002`

## 目标

- 把当前已有分析能力收口成正式洞察模块，并把 forecast / 外部协作边界定义清楚。

## 本次范围

- `analysis / expert` 能力产品化
- 报告读模型与摘要字段稳定化
- replay 分析链路契约收口
- forecast 抽象接口草案
- 外部协作接口草案

## 本次不做

- 不直接做 forecast 算法实现
- 不直接对接 `SGA-Web` / `SGA-EastFactory` / `Evole`
- 不改查询主链路
- 不改前端工作台和 CLI 壳层业务逻辑

## 完成标准

- 洞察模块返回结构可被前端 / CLI 直接消费
- 报告列表、报告详情、摘要字段边界清楚
- forecast 只保留正式抽象，不越权落算法
- 外部协作边界有清晰草案

## 依赖与协同

- 依赖 `agent-b` 已有 query history / replay / query result 证据链输出
- 完成后需给 `agent-d` 与 `agent-e` 提供稳定消费面

## 风险

- 如果把 forecast 与外部协作一起做深，本轮容易失控并扩散边界
