# agent-c 预测MVP实现与回测摘要指令单

- 指令单编号：`C-ORDER-20260419-1059-003`
- 时间：`2026-04-19 10:59:25 +08:00`
- agent：`agent-c`
- 授权级别：`模块内可使用 sub-agent`
- 对应工单：`C-TICKET-20260419-1059-003`

## 你要做什么

- 把当前 `forecast` 从 `contract_only` 推成最小可运行闭环。
- 基于 `agent-a` 提供的指标基础层，先实现内部指标预测 MVP。
- 输出正式预测结果 contract，至少包含预测点列、区间、假设、基础回测摘要。
- 让 `agent-e` 可以基于你的输出直接接入 `dc forecast`。

## 你不要做什么

- 不接 `SGA-Web` / `SGA-EastFactory` 外部数据。
- 不接 `Evole` 或其他优化器。
- 不直接上贝叶斯优化组合方案。
- 不为了“先进”而把 MVP 做成重算法研究项目。
- 不越权修改前端和 CLI 壳层业务逻辑。

## 本轮必须收口的点

1. 预测输入
- 来自正式 `metric_key`
- 时间范围、粒度、预测步数
- 可选基础过滤条件

2. 预测输出
- `forecast_id`
- `metric_key`
- `horizon`
- `points`
- `assumptions`
- `backtest_summary`
- `model_info`

3. 最小预测能力
- 先实现内部指标的 baseline 预测
- 要求可解释、可回归、可失败回报
- 不要求一步到位追求高精度

4. 回测摘要
- 至少给出基础误差指标或回放摘要
- 让调用方知道这个预测当前可信到什么程度

## 建议负责文件

- `doris-api/analyst_agent.py`
- `doris-api/main.py`
- 相关测试文件

## Sub-agent 授权

- 允许在 forecast MVP、回测摘要、测试补强范围内使用 sub-agent。
- 不允许 sub-agent 越权进入数据基础层定义、前端、CLI/MCP 壳层。

## 交付要求

- 改动文件清单
- 预测 MVP contract 说明
- baseline 方案说明
- 回测摘要说明
- 风险说明
- 回归命令与结果

## 验收标准

- `/api/analysis/forecast` 不再返回纯 `contract_only`
- 输出可被 `agent-e` 直接消费
- 预测失败时也有清晰、稳定的错误返回
