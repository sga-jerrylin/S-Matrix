# agent-a 数据基础层基建返工单

- 返工单编号：`A-REWORK-20260419-0949-001`
- 时间：`2026-04-19 09:49 +08:00`
- agent：`agent-a`
- 对应工单：`A-TICKET-20260419-0049-001`
- 对应审核单：`A-REVIEW-20260419-0949-001`

## 返工原因

- 关系稳定读模型在单表过滤场景下丢失了关系另一侧的展示名和字段展示名，导致 foundation profile 和 relationships 稳定读取面输出不完整。

## 必须修正

- 修正 `list_relationship_models()` 的上下文装配方式
- 当只请求某一张表时，仍需为关系另一侧表补齐 registry display name 和 field display name
- 确保 `get_table_profile()` 返回的 `relationships[*]` 中，对侧表和字段展示名完整
- 确保 `/api/relationships?tables=<table>` 返回的稳定关系模型也完整
- 增加至少 1 条回归测试，覆盖“单表过滤但关系跨两张表”的场景

## 禁止扩大范围

- 不要借返工扩展查询链路
- 不要顺手改前端、CLI、预测层
- 不要重做 foundation schema

## 再次提交要求

- 提交修复后的改动文件清单
- 提交新增/更新的测试清单
- 提交最小复现前后对比
- 提交本地测试结果
