# D-order-20260422-1523-003-查询工作台富响应与流式体验落地指令单

## 1. 指令主题

把升级后的 Vanna 2 rich UI / streaming 思路，落到 DorisClaw 的查询工作台里，但不直接照搬官方 `<vanna-chat>`。

## 2. 为什么由你负责

这是前端工作台层的事情，你已有上下文连续：

1. WorkbenchUpload / Query / Forecast
2. 工作台主路径信息架构
3. 洞察展示与查询工作台整合

## 3. 本次要落实的“升级后新能力”

### A. Rich query response layout

把查询结果从“单块返回”升级成可分区展示：

1. SQL 卡片
2. 结果表格卡片
3. 说明/解释卡片
4. trace/status 卡片
5. 可选图表卡片

### B. Progressive / streaming-like UX

即便后端还没完全切 SSE，也要先把前端交互壳做成 progressive。

要求：

1. 显示分阶段状态：
   - retrieval
   - sql generation
   - execution
   - finalize
2. 避免“长时间白屏等待”
3. 不发明后端未提供的数据字段；优先吃现有 trace / contract

### C. Artifact-ready query panel

参考 Vanna 2 rich component 思路，把前端结构做成后续可承接 richer artifact 的形态。

要求：

1. 表格和图表区域可并存
2. trace 区域可展开
3. 失败态 / fallback 态信息完整

## 4. 本次明确不做什么

1. 不直接嵌官方 `<vanna-chat>`
2. 不重做整站信息架构
3. 不改查询业务逻辑
4. 不改 CLI/MCP

## 5. 依赖关系

默认吃 `agent-B` 本轮新增的 retrieval trace / context contract。

如果 `agent-B` 尚未交付最终字段：

1. 先按当前稳定字段落结构壳
2. 明确哪些位置是 future-ready placeholder

## 6. 验收标准

必须全部满足：

1. 查询工作台不再只有单块结果区
2. progressive 状态可见
3. trace / error / fallback 可见
4. 页面结构可承接后续 richer artifact
5. 不破坏当前主路径

## 7. 返回格式

1. 改动文件清单
2. Vanna 2 rich UI 思路具体落了什么
3. 依赖了哪些后端字段
4. 构建/校验结果
5. 风险说明

