# agent-d 工作台主路径收口与洞察对齐指令单

- 指令单编号：`D-ORDER-20260419-1032-002`
- 时间：`2026-04-19 10:32:35 +08:00`
- agent：`agent-d`
- 授权级别：`模块内可使用 sub-agent`
- 对应工单：`D-TICKET-20260419-1032-002`

## 你要做什么

- 把当前前端从“功能堆叠”进一步收成“工作台主路径”。
- 主路径只围绕三件事：
  - 上传
  - 查询
  - 洞察
- 基于现有稳定接口，收口页面信息架构、主入口、结果展示结构。
- 与 `agent-c` 的洞察 contract 对齐，整理前端依赖 schema 清单。
- 为 forecast 留展示位，但本轮不做复杂预测交互。

## 你不要做什么

- 不反向驱动后端设计。
- 不自定义后端字段。
- 不越权重写查询逻辑、洞察逻辑、CLI/MCP 壳层。
- 不提前做复杂 forecast 页面。

## 本轮重点

1. 工作台结构
- 入口更清楚
- 任务路径更清楚
- 上传、查询、洞察之间的切换关系更清楚

2. 洞察页对齐
- 先吃稳 `agent-c` 收口后的 schema
- 不自己发明字段
- 不为适配现有页面把后端拖回去返工

3. 预测占位
- 只保留未来位置与信息架构
- 不设计重交互，不绑定未确定 contract

## Sub-agent 授权

- 允许在 `doris-frontend/src/App.vue`、`components/*`、`api/*` 范围内使用 sub-agent。
- 允许 sub-agent 分担页面结构整理与组件收口。
- 不允许 sub-agent 擅自定义后端 schema。

## 交付要求

- 提交工作台结构调整说明。
- 提交页面与组件改动清单。
- 提交依赖的后端 schema 清单。
- 提交风险说明。
- 提交回归或构建验证结果。

## 验收标准

- 页面不再像一组松散功能，而是能体现出 DorisClaw 的工作台主路径。
- 前端依赖关系清楚，不抢跑 forecast。
